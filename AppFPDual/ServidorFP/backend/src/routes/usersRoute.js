const { Router } = require('express');
const router = Router();

const UsersService = require('../services/usersService');

/*
router.post('/getUserByName', UsersService.getUserByName); 

router.post('/addUser', UsersService.addUser); 
*/

module.exports = router;